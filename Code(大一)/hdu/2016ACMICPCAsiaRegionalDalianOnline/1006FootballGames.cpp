#include <cstdio>
#include <fstream>
#include <cstring>
#include <algorithm>

using namespace std;

int main() {
	int n, num, a;
	scanf("%d", &n);
	for (int i = 0; i < n; ++i) {
		scanf("%d", &num);
	}
	return 0;
}
