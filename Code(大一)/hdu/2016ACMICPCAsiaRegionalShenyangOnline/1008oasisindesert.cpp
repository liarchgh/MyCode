#include <cstdio>
#include <algorithm>
#include <cstring>
#include <iostream>
#include <fstream>

using namespace std;

int n, m, k;

int main(){
	#ifndef ONLINE_JUDGE
	freopen("D:\\in.txt", "r", stdin);
	freopen("D:\\out.txt", "w", stdout);
	#endif
	int t;
	scanf("%d", &t);
	while(t--){
		scanf("%d%d%d\n", &n, &m, &k);
		int a, b, l;
		for(int i = 0; i < m; ++i){
			scanf("%d%d%d", &a, &b, &l);
		}
	}
	return 0;
}
