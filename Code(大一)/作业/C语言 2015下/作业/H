
#include <stdio.h>
int main()
{
	int m, i, k;
	scanf("%d", &m);
	for (i = 0; i < m; i++)
	{
		int n, a, b = 0;
		scanf("%d", &n);
		for (k = 0; k < n; k++)
		{
			scanf("%d", &a);
			b = b + a;
		}
		printf("%d\n\n", b);
	}
	return 0;
}