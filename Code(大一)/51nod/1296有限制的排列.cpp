#include <cstdio>
#include <algorithm>
#include <cstring>
#include <fstream>
#include <iostream>

using namespace std;

int n, k, l;

int main(){
	#ifndef ONLINE_JUDGE
	freopen("D:\\in.txt", "r", stdin);
	freopen("D:\\out.txt", "w", stdout);
	#endif
	scanf("%d%d%d", &n, &k, &l);
	for(int i = 0; i < k; ++i){}
	for(int i = 0; i < l; ++i){}
	return 0;
}
