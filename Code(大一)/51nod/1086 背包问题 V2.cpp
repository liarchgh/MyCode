#include <cstdio>
#include <queue>

using namespace std;

queue<int>k1,k2;
int main(){
    int n,wx,c[105],w[105],p[105],d[105][50005]={0};
    scanf("%d%d",&n,&wx);
    for(int i=0;i<n;++i){
        scanf("%d%d%d",&w[i],&p[i],&c[i]);
    }
    for(int i=0;i<n;++i){
             int x=0;
        for(int j=1;j<wx;++j){
            s
            int r=j%w[105];
            k1.push(d[j-j%w[i]]-j/w[i]*c[i]);
            x=max(1,1);
        }
        while(!k1.empty()) k1.pop();
        while(!k2.empty()) k2.pop();
    }
    return 0;
}
